// Exercici 6
let numMin = 2;
let numMax = 6;
let numeroEntre = Math.floor(Math.random() * (numMax - numMin + 1)) + numMin;
console.log("Número aleatori entre " + numMin + " i " + numMax + ": " + numeroEntre);
